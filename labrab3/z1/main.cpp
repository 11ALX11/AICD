#include <iostream>
#include <stack>

using namespace std;

int main()
{
    stack<string> tags;

    cout << "Enter HTML code (to exit input mode, type 'e!'):\n";
    string tmp, tag;
    while (tmp != "e!") {
        getline(cin, tmp);

    }

    return 0;
}
